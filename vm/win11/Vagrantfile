# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  #config.vm.box = "gusztavvargadr/windows-11"
  config.vm.box = "StefanScherer/windows_11"

  config.vm.network "public_network"
  config.vm.hostname = "vagrant-win"

  config.vm.communicator = "winrm"

  # Admin user name and password
  config.winrm.username = "vagrant"
  config.winrm.password = "vagrant"

  config.vm.guest = :windows
  config.vagrant.plugins = ["winrm", "winrm-fs", "winrm-elevated", "vagrant-vbguest"]
  # or install separately with: vagrant plugin install

  config.vm.synced_folder ".", "/vagrant", disabled: true
  config.vm.synced_folder "/home/patrick", "/home/patrick", create: true

  config.vm.provider "virtualbox" do |v|
    v.customize ['modifyvm', :id, '--clipboard', 'bidirectional']
    v.customize ['modifyvm', :id, '--draganddrop', 'bidirectional']
    v.gui = true
    v.name = "win11-vagrant"
    v.cpus = 4
    v.memory = 6144
  end
end
