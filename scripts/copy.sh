#!/bin/bash

# Up from scripts dir
cd ..

cp -r .ssh ${HOME}
cp -r .vim ${HOME}
cp .bash_profile ${HOME}
cp .bash_unsplash ${HOME}
cp .bashrc ${HOME}
cp .gitconfig ${HOME}
cp .gitmessage ${HOME}
cp .tmux.conf ${HOME}
cp .vimrc ${HOME}
cp .git-completion.bash ${HOME}
